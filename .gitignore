# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
.env
node_modules
dist
dist-ssr
*.local

# Generated by `npm run test:coverage`. It was committed once, which meant
# every coverage run showed up as ~30 modified files and CI made that worse
# by running coverage on every PR.
coverage

# Agent worktrees live here. A checkout nested inside the repo is invisible to
# git (it has its own .git) but very visible to any tool that globs the tree.
.claude/worktrees/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env.local
.env
*.env
.env
.env
.env.example
